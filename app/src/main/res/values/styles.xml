<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- <item name="colorPrimary">@color/colorPrimary</item>
         <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
         <item name="colorAccent">@color/colorAccent</item>
 -->
        <item name="colorPrimary">@color/ThemeFianl</item>
        <item name="colorPrimaryDark">@color/ThemeFianl</item>
        <item name="colorAccent">@color/ThemeFianl</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TabTextSize">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ThemeDialog">
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomDigitsThemenew" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:windowBackground">@drawable/background</item>
        <item name="android:textColorLink">#00B900</item>
        <item name="android:editTextStyle">@style/EditTextDefault</item>
        <item name="dgts__logoDrawable">@drawable/logo_small</item>
        <item name="colorAccent">#00B900</item>
    </style>

    <style name="EditTextDefault" parent="@android:style/Widget.EditText">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textColorHint">#ffffff</item>
        <item name="android:background">@drawable/my_custom_background</item>
    </style>

    <style name="Button" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/green</item>

        <!--    &lt;!&ndash; inactive thumb color &ndash;&gt;
            <item name="colorSwitchThumbNormal">@color/colorPrimary</item>

             inactive track color (30% transparency)
            <item name="android:colorForeground">@color/green</item>-->
    </style>

</resources>
